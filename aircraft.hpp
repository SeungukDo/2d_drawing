#ifndef AIRCRAFT_H
#define AIRCRAFT_H
#include <glm/glm.hpp>
#include <math.h>

void draw_polygon(int a, int b, int c , int d);
void draw_cube();
void draw_cuboid(float length, float width, float height);
void draw_pyramid();
void draw_aircraft();

#endif